# Sao chép file này thành `.env` rồi điền giá trị thật.
# `.env` đã nằm trong .gitignore — KHÔNG commit.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_APP_ENV=dev

# Khoá service_role KHÔNG bao giờ đặt ở đây.
# Nó chỉ nằm trong biến môi trường của Edge Function trên Supabase.
